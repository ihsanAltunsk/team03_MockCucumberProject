package pages.admin.bookingHistory;

import org.openqa.selenium.support.PageFactory;
import utilities.Driver;

public class RejectedTicket {
    public RejectedTicket(){
        PageFactory.initElements(Driver.getDriver(),this);}
}
